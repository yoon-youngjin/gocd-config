pipelines:
  test:
    group: dummy
    materials:
      dummy:
        plugin_configuration:
          id: github.pr
          version: 1.3.5
        options:
          url: "https://github.com/yoon-youngjin/dummy.git"
          base_branch: "main"
    stages:
      - test:
          jobs:
            test:
              tasks:
                - exec:
                    command: ./gradlew
                    arguments:
                      - test